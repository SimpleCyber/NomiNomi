{ market.description || `${market.name} is a community driven project on Cardano. Join the movement!` }
                </p >
              </div >
            </Link >
          ))}
        </div >
      )}
    </div >
  );
}
